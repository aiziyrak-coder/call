[general]
enabled = yes
pretty = yes
; Faqat telefoniya servisi (Docker tarmog'i) — brauzer emas.
allowed_origins = http://telephony:4100,http://api:4000

; Parol bind-mount ga yozilmaydi — runtime /var/run/asterisk/ari_auth.conf
#include "/var/run/asterisk/ari_auth.conf"
