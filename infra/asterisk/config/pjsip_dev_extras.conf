;==============================================================================
; Faqat development / apparatsiz sinov — prod compose bu faylni ulamaydi.
; MicroSIP/Zoiper: username=pstn-sim password=pstn_sim_dev
;==============================================================================

[pstn-sim]
type = endpoint
context = from-gsm
disallow = all
allow = ulaw,alaw,opus
auth = pstn-sim
aors = pstn-sim
direct_media = no
force_rport = yes
rewrite_contact = yes
callerid = Test Mijoz <+998900000001>
language = ru

[pstn-sim]
type = auth
auth_type = userpass
username = pstn-sim
password = pstn_sim_dev

[pstn-sim]
type = aor
max_contacts = 2
remove_existing = yes
